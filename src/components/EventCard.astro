---
interface Props {
  title: string;
  startTime: string;
  endTime: string;
}

const { title, startTime, endTime } = Astro.props;
const timeFormatter = new Intl.DateTimeFormat('en-US', {
  hour: 'numeric',
  minute: '2-digit',
  hour12: true
});
const start = new Date(startTime);
const end = new Date(endTime);
const range = `${timeFormatter.format(start)} - ${timeFormatter.format(end)}`;
---

<div
  data-slot="card"
  class="bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm w-full max-w-sm min-w-sm"
>
  <div
    data-slot="card-header"
    class="@container/card-header grid auto-rows-min grid-rows-[auto_auto] items-start gap-1.5 px-6 has-data-[slot=card-action]:grid-cols-[1fr_auto] [.border-b]:pb-6"
  >
    <div data-slot="card-title" class="leading-none font-semibold">
      {title}
    </div><div
      data-slot="card-description"
      class="text-muted-foreground text-sm"
    >
      {range}
    </div><div data-slot="card-content" class="px-6"></div>
  </div>
</div>
